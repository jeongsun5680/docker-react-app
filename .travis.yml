#sudo: required

os : linux

language: generic

dist : xenial

services: 
  - docker

before_install:
  - echo "start creating an image with dockerfile"
  - docker build -t jeong5680/docker-react-app -f Dockerfile.dev .

script:
  - docker run -e CI=true jeong5690/docker-react-app npm run test -- --coverage

after_success:
  - echo "Test success"